// Code generated by `deno task db:gen-mirror`. DO NOT EDIT.
//
// Source of truth: src/mirror/endpoints.ts. Layer 1 (canonical) of
// docs/PLAN-igdb-mirror.md — a 1:1 mirror of IGDB's data dumps that is never
// hand-edited. Every augmentation lives downstream in the derived layer.

import {
	bigint,
	boolean,
	doublePrecision,
	index,
	integer,
	pgTable,
	text,
	timestamp,
	uuid,
} from 'drizzle-orm/pg-core'

export const igdbGames = pgTable(
	'igdb_games',
	{
		id: bigint('id', { mode: 'number' }).primaryKey(),
		name: text('name'),
		slug: text('slug'),
		url: text('url'),
		createdAt: timestamp('created_at', { withTimezone: true }),
		updatedAt: timestamp('updated_at', { withTimezone: true }),
		summary: text('summary'),
		storyline: text('storyline'),
		collection: bigint('collection', { mode: 'number' }),
		franchise: bigint('franchise', { mode: 'number' }),
		franchises: bigint('franchises', { mode: 'number' }).array(),
		hypes: integer('hypes'),
		follows: integer('follows'),
		rating: doublePrecision('rating'),
		aggregatedRating: doublePrecision('aggregated_rating'),
		aggregatedRatingCount: integer('aggregated_rating_count'),
		totalRating: doublePrecision('total_rating'),
		totalRatingCount: integer('total_rating_count'),
		ratingCount: integer('rating_count'),
		parentGame: bigint('parent_game', { mode: 'number' }),
		versionParent: bigint('version_parent', { mode: 'number' }),
		versionTitle: text('version_title'),
		similarGames: bigint('similar_games', { mode: 'number' }).array(),
		gameEngines: bigint('game_engines', { mode: 'number' }).array(),
		playerPerspectives: bigint('player_perspectives', { mode: 'number' }).array(),
		gameModes: bigint('game_modes', { mode: 'number' }).array(),
		keywords: bigint('keywords', { mode: 'number' }).array(),
		themes: bigint('themes', { mode: 'number' }).array(),
		genres: bigint('genres', { mode: 'number' }).array(),
		expansions: bigint('expansions', { mode: 'number' }).array(),
		dlcs: bigint('dlcs', { mode: 'number' }).array(),
		bundles: bigint('bundles', { mode: 'number' }).array(),
		standaloneExpansions: bigint('standalone_expansions', { mode: 'number' }).array(),
		firstReleaseDate: timestamp('first_release_date', { withTimezone: true }),
		status: integer('status'),
		platforms: bigint('platforms', { mode: 'number' }).array(),
		releaseDates: bigint('release_dates', { mode: 'number' }).array(),
		alternativeNames: bigint('alternative_names', { mode: 'number' }).array(),
		screenshots: bigint('screenshots', { mode: 'number' }).array(),
		videos: bigint('videos', { mode: 'number' }).array(),
		cover: bigint('cover', { mode: 'number' }),
		websites: bigint('websites', { mode: 'number' }).array(),
		externalGames: bigint('external_games', { mode: 'number' }).array(),
		multiplayerModes: bigint('multiplayer_modes', { mode: 'number' }).array(),
		involvedCompanies: bigint('involved_companies', { mode: 'number' }).array(),
		ageRatings: bigint('age_ratings', { mode: 'number' }).array(),
		artworks: bigint('artworks', { mode: 'number' }).array(),
		checksum: uuid('checksum'),
		remakes: bigint('remakes', { mode: 'number' }).array(),
		remasters: bigint('remasters', { mode: 'number' }).array(),
		expandedGames: bigint('expanded_games', { mode: 'number' }).array(),
		ports: bigint('ports', { mode: 'number' }).array(),
		forks: bigint('forks', { mode: 'number' }).array(),
		languageSupports: bigint('language_supports', { mode: 'number' }).array(),
		gameLocalizations: bigint('game_localizations', { mode: 'number' }).array(),
		collections: bigint('collections', { mode: 'number' }).array(),
		gameStatus: bigint('game_status', { mode: 'number' }),
		gameType: bigint('game_type', { mode: 'number' }),
		redirectGameId: bigint('redirect_game_id', { mode: 'number' }),
		mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
		deletedAt: timestamp('deleted_at', { withTimezone: true }),
	},
	(t) => [
		index('igdb_games_slug_idx').on(t.slug),
		index('igdb_games_parent_game_idx').on(t.parentGame),
		index('igdb_games_version_parent_idx').on(t.versionParent),
		index('igdb_games_game_type_idx').on(t.gameType),
		index('igdb_games_updated_at_idx').on(t.updatedAt),
	],
)

export const igdbCovers = pgTable(
	'igdb_covers',
	{
		id: bigint('id', { mode: 'number' }).primaryKey(),
		url: text('url'),
		imageId: text('image_id'),
		width: integer('width'),
		height: integer('height'),
		alphaChannel: boolean('alpha_channel'),
		animated: boolean('animated'),
		game: bigint('game', { mode: 'number' }),
		checksum: uuid('checksum'),
		gameLocalization: bigint('game_localization', { mode: 'number' }),
		imageType: bigint('image_type', { mode: 'number' }),
		mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
		deletedAt: timestamp('deleted_at', { withTimezone: true }),
	},
	(t) => [index('igdb_covers_game_idx').on(t.game)],
)

export const igdbPlatforms = pgTable('igdb_platforms', {
	id: bigint('id', { mode: 'number' }).primaryKey(),
	name: text('name'),
	slug: text('slug'),
	url: text('url'),
	createdAt: timestamp('created_at', { withTimezone: true }),
	updatedAt: timestamp('updated_at', { withTimezone: true }),
	summary: text('summary'),
	platformFamily: bigint('platform_family', { mode: 'number' }),
	alternativeName: text('alternative_name'),
	generation: integer('generation'),
	versions: bigint('versions', { mode: 'number' }).array(),
	abbreviation: text('abbreviation'),
	platformLogo: bigint('platform_logo', { mode: 'number' }),
	websites: bigint('websites', { mode: 'number' }).array(),
	checksum: uuid('checksum'),
	platformType: bigint('platform_type', { mode: 'number' }),
	mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
	deletedAt: timestamp('deleted_at', { withTimezone: true }),
})

export const igdbGenres = pgTable('igdb_genres', {
	id: bigint('id', { mode: 'number' }).primaryKey(),
	name: text('name'),
	createdAt: timestamp('created_at', { withTimezone: true }),
	updatedAt: timestamp('updated_at', { withTimezone: true }),
	slug: text('slug'),
	url: text('url'),
	checksum: uuid('checksum'),
	mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
	deletedAt: timestamp('deleted_at', { withTimezone: true }),
})

export const igdbCompanies = pgTable('igdb_companies', {
	id: bigint('id', { mode: 'number' }).primaryKey(),
	name: text('name'),
	createdAt: timestamp('created_at', { withTimezone: true }),
	updatedAt: timestamp('updated_at', { withTimezone: true }),
	slug: text('slug'),
	url: text('url'),
	logo: bigint('logo', { mode: 'number' }),
	description: text('description'),
	startDate: timestamp('start_date', { withTimezone: true }),
	startDateCategory: integer('start_date_category'),
	country: integer('country'),
	parent: bigint('parent', { mode: 'number' }),
	changedCompanyId: bigint('changed_company_id', { mode: 'number' }),
	changeDate: timestamp('change_date', { withTimezone: true }),
	changeDateCategory: integer('change_date_category'),
	twitter: text('twitter'),
	facebook: text('facebook'),
	website: bigint('website', { mode: 'number' }),
	websites: bigint('websites', { mode: 'number' }).array(),
	checksum: uuid('checksum'),
	status: bigint('status', { mode: 'number' }),
	startDateFormat: bigint('start_date_format', { mode: 'number' }),
	changeDateFormat: bigint('change_date_format', { mode: 'number' }),
	companySize: bigint('company_size', { mode: 'number' }),
	mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
	deletedAt: timestamp('deleted_at', { withTimezone: true }),
})

export const igdbInvolvedCompanies = pgTable(
	'igdb_involved_companies',
	{
		id: bigint('id', { mode: 'number' }).primaryKey(),
		createdAt: timestamp('created_at', { withTimezone: true }),
		updatedAt: timestamp('updated_at', { withTimezone: true }),
		game: bigint('game', { mode: 'number' }),
		company: bigint('company', { mode: 'number' }),
		publisher: boolean('publisher'),
		developer: boolean('developer'),
		supporting: boolean('supporting'),
		porting: boolean('porting'),
		checksum: uuid('checksum'),
		mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
		deletedAt: timestamp('deleted_at', { withTimezone: true }),
	},
	(t) => [
		index('igdb_involved_companies_game_idx').on(t.game),
		index('igdb_involved_companies_company_idx').on(t.company),
	],
)

export const igdbWebsites = pgTable(
	'igdb_websites',
	{
		id: bigint('id', { mode: 'number' }).primaryKey(),
		url: text('url'),
		trusted: boolean('trusted'),
		game: bigint('game', { mode: 'number' }),
		checksum: uuid('checksum'),
		type: bigint('type', { mode: 'number' }),
		mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
		deletedAt: timestamp('deleted_at', { withTimezone: true }),
	},
	(t) => [index('igdb_websites_game_idx').on(t.game)],
)

export const igdbExternalGames = pgTable(
	'igdb_external_games',
	{
		id: bigint('id', { mode: 'number' }).primaryKey(),
		name: text('name'),
		createdAt: timestamp('created_at', { withTimezone: true }),
		updatedAt: timestamp('updated_at', { withTimezone: true }),
		uid: text('uid'),
		year: integer('year'),
		url: text('url'),
		game: bigint('game', { mode: 'number' }),
		checksum: uuid('checksum'),
		countries: integer('countries').array(),
		platform: bigint('platform', { mode: 'number' }),
		media: integer('media'),
		externalGameSource: bigint('external_game_source', { mode: 'number' }),
		gameReleaseFormat: bigint('game_release_format', { mode: 'number' }),
		mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
		deletedAt: timestamp('deleted_at', { withTimezone: true }),
	},
	(t) => [
		index('igdb_external_games_game_idx').on(t.game),
		index('igdb_external_games_uid_idx').on(t.uid),
	],
)

export const igdbAlternativeNames = pgTable(
	'igdb_alternative_names',
	{
		id: bigint('id', { mode: 'number' }).primaryKey(),
		name: text('name'),
		comment: text('comment'),
		game: bigint('game', { mode: 'number' }),
		checksum: uuid('checksum'),
		mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
		deletedAt: timestamp('deleted_at', { withTimezone: true }),
	},
	(t) => [index('igdb_alternative_names_game_idx').on(t.game)],
)

export const igdbGameLocalizations = pgTable(
	'igdb_game_localizations',
	{
		id: bigint('id', { mode: 'number' }).primaryKey(),
		name: text('name'),
		cover: bigint('cover', { mode: 'number' }),
		game: bigint('game', { mode: 'number' }),
		region: bigint('region', { mode: 'number' }),
		createdAt: timestamp('created_at', { withTimezone: true }),
		updatedAt: timestamp('updated_at', { withTimezone: true }),
		checksum: uuid('checksum'),
		mirrorUpdatedAt: timestamp('mirror_updated_at', { withTimezone: true }).notNull().defaultNow(),
		deletedAt: timestamp('deleted_at', { withTimezone: true }),
	},
	(t) => [index('igdb_game_localizations_game_idx').on(t.game)],
)
